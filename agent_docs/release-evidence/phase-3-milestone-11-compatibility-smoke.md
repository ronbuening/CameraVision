# Phase 3 Milestone 11 Compatibility Smoke

Date: 2026-06-25
Status: complete

This note records the Phase 3 Milestone 11 compatibility-smoke result for normalized XMP sidecars written through the Phase 2 owned XMP writer path.

## Result

Phase 3 normalized export is accepted as complete for release-evidence purposes. Project-owner manual checks confirmed working normalized XMP examples with Lightroom Classic and Capture One, and reported the exercised Phase 3 command flags passed.

The Lightroom Classic and Capture One examples are owner-local smoke artifacts and are not committed to this repository. This evidence therefore records the compatibility result, not a checked-in fixture set.

## Local Verification

Codex recorded the following local checks on 2026-06-25:

- `swift test`: passed, 330 tests, 1 skipped, 0 failures.
- `swift run aisidecar --help`: passed.
- `swift run aisidecar write-xmp --help`: passed.
- `swift run aisidecar normalize --help`: passed.
- `swift run aisidecar apply-session --help`: passed.
- `swift run aisidecar benchmark --self-test`: passed, writing `benchmarks/milestone9a-self-test-76B9D237-91EC-4A91-83DC-406FF8310867`.

The test suite includes the linked no-XMP regression coverage, normalized write and dry-run artifact behavior, apply-session source staleness checks, current-XMP merge behavior, parser readback, malformed/unsupported XMP failure modes, and Phase 3 affinity/consensus/privacy/reporting baselines.

## Manual Compatibility Evidence

Manual owner evidence covers:

- normalized XMP files generated by the project-owned writer path;
- Lightroom Classic readback of the generated XMP keywords;
- Capture One readback of the generated XMP keywords;
- representative Phase 3 flags exercised successfully, including normalized session/dry-run/write and apply-session paths.

## Deferred Release-Signoff Evidence

Phase 1 Milestone 9 calibration and final model-quality review remain separate release-signoff evidence. That deferral does not change the Phase 3 compatibility result; the residual risk is model-output quality calibration, not normalized XMP writing, parser compatibility, or Phase 3 command behavior.

Representative GPS-context model-quality comparison remains tied to that Phase 1 quality review unless a release explicitly treats GPS quality as in-scope. Phase 3 preserves the GPS boundary: GPS may influence model prompts and raw sidecar provenance, but GPS-only evidence and coordinates are not exported as XMP keywords.
